apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

configMapGenerator:
  - name: oauth-proxy-vault-config
    files: 
      - vault-agent.hcl

resources:
  - deployment.yaml
  - service.yaml
