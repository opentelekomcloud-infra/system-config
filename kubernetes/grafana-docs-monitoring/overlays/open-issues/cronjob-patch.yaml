apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: grafana-docs-monitoring-open-issues
spec:
  schedule: "30 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            command:
            - /bin/sh
            - -c
            - python3 open_issues.py