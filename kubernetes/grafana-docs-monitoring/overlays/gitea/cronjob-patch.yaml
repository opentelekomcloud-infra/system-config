apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: grafana-docs-monitoring-gitea
spec:
  schedule: "15 * * * *"
  jobTemplate:
  spec:
    template:
      spec:
        containers:
          command:
          - /bin/sh
          - -c
          - python3 gitea_info.py