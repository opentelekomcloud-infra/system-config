apiVersion: v1
kind: Service
metadata:
  name: {{ include "redis-minimal.fullname" . }}
  labels:
    {{- include "redis-minimal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    {{- include "redis-minimal.selectorLabels" . | nindent 4 }}
