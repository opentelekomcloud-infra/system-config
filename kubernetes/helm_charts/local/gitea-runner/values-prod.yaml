replicaCount: 1

image:
  repository: "gitea/act_runner"
  tag: "0.2.6"
  pullPolicy: IfNotPresent

gitea:
  instanceUrl: "https://gitea-k8s.eco.tsi-dev.otc-service.com"
  # Org-level registration token for the 'infra' org on gitea-k8s
  # (serves all repos within the infra organization)
  registrationToken: "<path:secret/data/gitea/gitea-runner#runner_registration_token>"

runner:
  name: "gitea-k8s-runner-01"
  labels: "ubuntu:docker://node:20-bookworm,ubuntu-22.04:docker://node:20-bookworm,debian:docker://node:20-bookworm,debian-bookworm:docker://node:20-bookworm"
  capacity: 4
  logLevel: "info"

resources:
  limits:
    cpu: 2000m
    memory: 4Gi
  requests:
    cpu: 100m
    memory: 512Mi

persistence:
  enabled: false
  storageClass: "csi-disk"
  accessMode: ReadWriteOnce
  size: 5Gi


podAnnotations:
  prometheus.io/scrape: "false"

podLabels:
  environment: "prod"
  component: "gitea-runner"

serviceAccount:
  create: true

dind:
  image:
    repository: "docker"
    tag: "24.0.7-dind"
    pullPolicy: IfNotPresent
  storageDriver: "overlay2"

securityContext:
  privileged: true
