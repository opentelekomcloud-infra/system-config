apiVersion: v1
kind: Secret
metadata:
  name: zitadel-masterkey
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
    {{- include "zitadel.labels" . | nindent 4 }}
type: Opaque
stringData:
  masterkey: {{ .Values.masterkey | nindent 4 }}
