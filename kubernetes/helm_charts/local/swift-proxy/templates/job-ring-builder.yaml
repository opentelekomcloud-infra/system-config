{{- if eq .Values.mode "cluster" }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "swift-proxy.fullname" . }}-ring-builder
  labels:
    {{- include "swift-proxy.labels" . | nindent 4 }}
    app.kubernetes.io/component: ring-builder
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  template:
    metadata:
      labels:
        {{- include "swift-proxy.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: ring-builder
    spec:
      restartPolicy: OnFailure
      serviceAccountName: {{ include "swift-proxy.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: ring-builder
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.proxy.image.repository }}:{{ .Values.proxy.image.tag }}"
          imagePullPolicy: {{ .Values.proxy.image.pullPolicy }}
          command: ["/bin/bash", "-c"]
          args:
            - |
              set -e
              echo "Building Swift rings..."

              cd /etc/swift

              # Create account ring
              swift-ring-builder account.builder create {{ .Values.swift.partitionPower }} {{ .Values.swift.replicaFactor }} {{ .Values.swift.minPartHours }}
              {{- range $i := until (int .Values.storage.replicas) }}
              swift-ring-builder account.builder add \
                --region 1 --zone {{ add $i 1 }} \
                --ip {{ include "swift-proxy.fullname" $ }}-storage-{{ $i }}-0.{{ include "swift-proxy.fullname" $ }}-storage.{{ $.Release.Namespace }}.svc.cluster.local \
                --port {{ $.Values.storage.ports.account }} \
                --device sda --weight 100
              {{- end }}
              swift-ring-builder account.builder rebalance

              # Create container ring
              swift-ring-builder container.builder create {{ .Values.swift.partitionPower }} {{ .Values.swift.replicaFactor }} {{ .Values.swift.minPartHours }}
              {{- range $i := until (int .Values.storage.replicas) }}
              swift-ring-builder container.builder add \
                --region 1 --zone {{ add $i 1 }} \
                --ip {{ include "swift-proxy.fullname" $ }}-storage-{{ $i }}-0.{{ include "swift-proxy.fullname" $ }}-storage.{{ $.Release.Namespace }}.svc.cluster.local \
                --port {{ $.Values.storage.ports.container }} \
                --device sda --weight 100
              {{- end }}
              swift-ring-builder container.builder rebalance

              # Create object ring
              swift-ring-builder object.builder create {{ .Values.swift.partitionPower }} {{ .Values.swift.replicaFactor }} {{ .Values.swift.minPartHours }}
              {{- range $i := until (int .Values.storage.replicas) }}
              swift-ring-builder object.builder add \
                --region 1 --zone {{ add $i 1 }} \
                --ip {{ include "swift-proxy.fullname" $ }}-storage-{{ $i }}-0.{{ include "swift-proxy.fullname" $ }}-storage.{{ $.Release.Namespace }}.svc.cluster.local \
                --port {{ $.Values.storage.ports.object }} \
                --device sda --weight 100
              {{- end }}
              swift-ring-builder object.builder rebalance

              echo "Rings built successfully!"
              echo "Account ring:"
              swift-ring-builder account.builder
              echo ""
              echo "Container ring:"
              swift-ring-builder container.builder
              echo ""
              echo "Object ring:"
              swift-ring-builder object.builder

              # Create ConfigMap with ring files
              echo "Creating ConfigMap with ring files..."

              # Use curl to create ConfigMap via Kubernetes API
              TOKEN=$(cat /var/run/secrets/kubernetes.io/serviceaccount/token)
              CACERT=/var/run/secrets/kubernetes.io/serviceaccount/ca.crt
              NAMESPACE=$(cat /var/run/secrets/kubernetes.io/serviceaccount/namespace)

              # Create ConfigMap YAML
              cat > /tmp/configmap.yaml <<EOF
              apiVersion: v1
              kind: ConfigMap
              metadata:
                name: {{ include "swift-proxy.fullname" . }}-rings
                namespace: $NAMESPACE
              binaryData:
                account.ring.gz: $(base64 -w 0 account.ring.gz)
                container.ring.gz: $(base64 -w 0 container.ring.gz)
                object.ring.gz: $(base64 -w 0 object.ring.gz)
                account.builder: $(base64 -w 0 account.builder)
                container.builder: $(base64 -w 0 container.builder)
                object.builder: $(base64 -w 0 object.builder)
              EOF

              # Apply via Kubernetes API
              curl --cacert $CACERT -H "Authorization: Bearer $TOKEN" \
                -H "Content-Type: application/yaml" \
                -X POST \
                https://kubernetes.default.svc/api/v1/namespaces/$NAMESPACE/configmaps \
                --data-binary @/tmp/configmap.yaml || \
              curl --cacert $CACERT -H "Authorization: Bearer $TOKEN" \
                -H "Content-Type: application/yaml" \
                -X PUT \
                https://kubernetes.default.svc/api/v1/namespaces/$NAMESPACE/configmaps/{{ include "swift-proxy.fullname" . }}-rings \
                --data-binary @/tmp/configmap.yaml

              echo "Ring ConfigMap created successfully!"
          volumeMounts:
            - name: ring-workspace
              mountPath: /etc/swift
            - name: swift-config
              mountPath: /etc/swift-base
              readOnly: true
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
      volumes:
        - name: ring-workspace
          emptyDir: {}
        - name: swift-config
          configMap:
            name: {{ include "swift-proxy.fullname" . }}-config
{{- end }}
