---
# Kubernetes Replicator configuration for prod
# Automatically replicates secrets/configmaps across namespaces

kubernetes-replicator:
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 200m
      memory: 256Mi

  # Watch all namespaces
  watchNamespace: ""
