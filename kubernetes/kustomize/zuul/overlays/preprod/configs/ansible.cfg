[defaults]
# Enable pipelining to avoid temp directory issues with kubectl connection plugin
# This streams Python modules to stdin instead of writing them to temp files
pipelining = True

[ssh_connection]
# Also enable pipelining for SSH connections
pipelining = True
