---
# Site-specific variables for Zuul executor

# Log storage configuration - Using local NFS storage
# Swift upload disabled - logs stored on local zuul-config PVC mounted at /var/lib/zuul/builds
zuul_site_upload_logs: false

# Log retention on local storage
zuul_log_path: "{{ zuul.executor.log_root }}"

