---
# PV for Zuul scheduler keys persistence (SFS Turbo NFS)
# TODO: Update NFS server IP when prod SFS Turbo is provisioned
apiVersion: v1
kind: PersistentVolume
metadata:
  name: zuul-prod-scheduler-keys-sfs-turbo
spec:
  capacity:
    storage: 100Mi
  accessModes:
    - ReadWriteOnce
  storageClassName: csi-sfsturbo
  persistentVolumeReclaimPolicy: Retain
  mountOptions:
    - vers=3
    - nolock
    - noresvport
    - noatime
    - nodiratime
    - rsize=1048576
    - wsize=1048576
    - timeo=600
    - retrans=2
    - hard
    - actimeo=120
  nfs:
    path: /zuul-scheduler-keys
    server: PLACEHOLDER_SFS_TURBO_IP  # TODO: Replace with actual SFS Turbo NFS server IP
