apiVersion: v1
kind: Service
metadata:
  name: openapi-validator-svc
  labels:
    app: openapi-validator
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3000
    targetPort: http
    protocol: TCP
  selector:
    app: openapi-validator
