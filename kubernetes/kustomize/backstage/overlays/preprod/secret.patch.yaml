---
apiVersion: v1
kind: Secret
metadata:
  name: backstage-secret
type: Opaque
stringData:
  app-config.preprod.yaml: |
    catalog:
      providers:
        zitadelOrg:
          default:
            projectId: ${ZITADEL_PROJECT_ID}
            clientId: ${ZITADEL_CLIENT_ID}
            clientSecret: ${ZITADEL_CLIENT_SECRET}

    # kubernetes:
    #   serviceLocatorMethod:
    #     type: 'multiTenant'
    #   clusterLocatorMethods:
    #     - type: 'config'
    #       clusters:
    #         - name: preprod
    #           authProvider: 'serviceAccount'
    #           skipTLSVerify: true
    #           skipMetricsLookup: true
    #           url: ''
    #           serviceAccountToken: ''
