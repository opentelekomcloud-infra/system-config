apiVersion: v1
kind: Service
metadata:
  name: gitea-valkey-headless
  labels:
    app.kubernetes.io/component: valkey
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/component: valkey
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: gitea-valkey
  labels:
    app.kubernetes.io/component: valkey
spec:
  selector:
    app.kubernetes.io/component: valkey
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
