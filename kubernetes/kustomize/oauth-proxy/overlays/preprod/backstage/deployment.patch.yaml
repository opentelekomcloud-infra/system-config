---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: oauth-proxy
spec:
  selector:
    matchLabels:
      app.kubernetes.io/component: oauth-proxy
  template:
    metadata:
      labels:
        app.kubernetes.io/component: oauth-proxy
    spec:
      serviceAccount: oauth-proxy
