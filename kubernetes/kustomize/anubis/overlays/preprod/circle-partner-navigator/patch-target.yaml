apiVersion: apps/v1
kind: Deployment
metadata:
  name: anubis
spec:
  template:
    spec:
      containers:
        - name: anubis
          env:
            - name: TARGET
              value: "http://cpn-front:80"
