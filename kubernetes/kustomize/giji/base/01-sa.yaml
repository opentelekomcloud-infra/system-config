apiVersion: v1
kind: ServiceAccount
metadata:
  name: giji
  namespace: giji
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: giji-role-tokenreview-binding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: system:auth-delegator  # Required for Vault Kubernetes auth - allows the SA to review tokens
subjects:
  - kind: ServiceAccount
    name: giji
    namespace: giji
