apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

configMapGenerator:
  - name: giji-vault-config
    files:
      - vault-agent.hcl

images:
  - name: "quay.io/opentelekomcloud/giji"
    newTag: "0.1.0"
