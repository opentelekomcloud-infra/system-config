apiVersion: v1
kind: Service
metadata:
  name: llamacpp-api
  labels:
    app: llamacpp
spec:
  type: ClusterIP
  selector:
    app: llamacpp
  ports:
    - name: http
      port: 8000
      targetPort: 8080
      protocol: TCP