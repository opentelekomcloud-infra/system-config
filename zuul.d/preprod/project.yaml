---
- project:
    name: opentelekomcloud-infra/system-config
    merge-mode: squash-merge
    check:
      jobs:
        - otc-tox-linters:
            nodeset: zuul-debian
            branches: ^([^m].*|m[^a].*|ma[^i].*|mai[^n].*)$
        - otc-tox-docs:
            nodeset: zuul-debian
            branches: ^([^m].*|m[^a].*|ma[^i].*|mai[^n].*)$
    gate:
      jobs:
        - otc-tox-linters:
            nodeset: zuul-debian
            branches: ^([^m].*|m[^a].*|ma[^i].*|mai[^n].*)$
        - otc-tox-docs:
            nodeset: zuul-debian
            branches: ^([^m].*|m[^a].*|ma[^i].*|mai[^n].*)$
    post:
      jobs:
        - project-cleanup
