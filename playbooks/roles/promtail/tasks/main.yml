---
- name: Include variables
  ansible.builtin.include_vars: "{{ lookup('first_found', params) }}"
  vars:
    params:
      files: "{{ distro_lookup_path }}"
      paths:
        - "vars"

- name: Remove conflicting Ubuntu Docker packages
  become: true
  ansible.builtin.package:
    state: absent
    name: "{{ remove_packages }}"
  when: "ansible_facts.pkg_mgr != 'atomic_container'"
  register: remove_result
  until: remove_result is success
  retries: 5

- name: Install required packages
  become: true
  ansible.builtin.package:
    state: present
    name: "{{ packages }}"
  when: "ansible_facts.pkg_mgr != 'atomic_container'"
  register: install_result
  until: install_result is success
  retries: 5

- name: Clean up stale sockets after removing legacy packages
  become: true
  block:
    - name: Stop Docker service and socket
      ansible.builtin.systemd:
        name: "{{ item }}"
        state: stopped
      loop:
        - docker.service
        - docker.socket
      failed_when: false

    - name: Remove stale Docker socket and PID files
      ansible.builtin.file:
        path: "{{ item }}"
        state: absent
      loop:
        - /var/run/docker.sock
        - /var/run/docker.pid

    - name: Restart containerd to reset socket bindings
      ansible.builtin.systemd:
        name: containerd
        state: restarted
        enabled: true
  when: remove_result is changed

- name: Start and enable Docker socket and daemon
  become: true
  ansible.builtin.systemd:
    name: "{{ item }}"
    state: started
    enabled: true
  loop:
    - docker.socket
    - docker.service

- name: Make sure promtail conf dir exists
  become: true
  ansible.builtin.file:
    path: "/etc/promtail"
    state: directory
    mode: '0755'

- name: Write promtail conf
  become: true
  ansible.builtin.template:
    src: "config.yml.j2"
    dest: "/etc/promtail/config.yml"
    mode: '0644'
  notify:
    - restart promtail

- name: Write promtail systemd unit file
  become: true
  ansible.builtin.template:
    src: "promtail.service.j2"
    dest: "{{ promtail_systemd_unit_path }}"
    mode: '0644'
  notify:
    - restart promtail
